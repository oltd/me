# The URL the site will be built for
base_url = "https://oltd.dev"

title = "oltd"
description = "person blog for µControllers, Programming and Cloud stuff"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# english will be the default language
default_lang = "en"

# choose my own theme from the submodule
theme = "dose"

[markdown]
# we don't need that. yet.
highlight_code = false

# making our external links behave correctly
external_links_target_blank = true
external_links_no_follow = true
external_links_no_referrer = true

# who invented this? just stupid.
smart_punctuation = false

[extra]
author = "oltd"
github = "oltd"
twitter = "oltd_maker"
show_me = true